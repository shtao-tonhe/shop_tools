import { UseroptlogInterceptor } from './useroptlog.interceptor';

describe('UseroptlogInterceptor', () => {
  it('should be defined', () => {
    expect(new UseroptlogInterceptor()).toBeDefined();
  });
});
